import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    listMaker: {
        margin: 10,
        marginTop: 60,
        flexDirection: 'row',
        alignItems: 'center'
    },
    textMarker: {
        marginLeft: 5,
        fontSize: 20
    }
})